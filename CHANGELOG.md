# Changelog

## v1.0.0-preview - 2026-08-13

最后一个私有内测预览版本；下一阶段进入公开测试。

### Added

- 模型级 API 协议切换：Responses、Chat Completions、Anthropic Messages。
- “测试当前 / 测试全部”协议诊断，以及协议对应的 Base URL、完整端点和鉴权方式提示。
- 侧栏本地模型实时状态：已关闭、正在加载、已就绪、输出中、正在停止、外部服务和异常。
- 三类协议的真实 HTTP 回环测试，以及协议切换与自定义命令隔离的交互回归。

### Changed

- 侧栏不再显示容易混淆的“安装模式 / 便携模式”；安装包类型仍保留，但不再被误作运行状态。
- 托管 API Key 统一为 `sk-llamalift-<64 位小写十六进制>`，随机部分使用 32 字节密码学安全随机数。
- API 测试请求缩短输出上限以减少诊断等待，同时保留完整错误响应供排查。
- 界面继续遵循 Apple 式克制层级，并为协议摘要、单行快捷操作和高 DPI 文本预留响应空间。

### Fixed

- 修复切换客户端协议时意外退出自定义命令模式的问题。
- 修复协议测试过程中切换模型配置可能造成结果与界面目标错位的问题；测试现在使用启动时快照。
- 修复 API Key 文件不可读时异常冒泡而缺少明确诊断的问题。

### Tests

- 离线套件覆盖协议路由、JSON 请求体、Bearer/x-api-key 鉴权、真实 loopback、配置迁移、API Key 格式、参数同步和进程生命周期。
- 22 个 UI 场景覆盖七页、940×600、1320×840、浅色/深色和 125%/150%/175%/200% DPI，并模拟协议切换与输出状态。

## v0.4.0-dev - 2026-08-12

私有开发版本，尚未发布。

### Added

- llama-server 本机自动检测：优先识别已登记运行时、现有配置、PATH、LlamaLift 数据目录和常见安装目录，确认后自动填写；未找到或识别错误时可手动选择安装目录或程序文件。
- API Key 启动兼容层：启动前真实验证文件可读性，并为不支持中文参数路径的 llama-server 构建建立临时兼容副本。

### Changed

- 快速、均衡、极限固定预设统一为 32K、64K、128K；自适应方案根据 GGUF 上限、硬件资源和运行时真实 KV 能力安全调整。
- GPU 监测改为 2 秒批量读取，离开性能页后自动降频，减少空闲开销。
- 服务生命周期改为异步、会话化管理；停止、重启、端口释放和窗口关闭采用统一状态门禁。
- 日志使用有界队列批量刷新和落盘，降低高吞吐日志对界面响应的影响。
- 服务总览操作固定为“重启 / 停止 / 启动服务”单行布局，并继续完善 Apple 式圆角与响应式细节。

### Fixed

- 修复性能档位与上下文、KV Cache、Batch 等表单字段显示与实际配置不一致；手工改参会明确标记为“自定义”。
- 修复仅支持 turbo3 或仅支持 turbo4 的 TurboQuant 运行时生成不受支持 KV 类型的问题。
- 修复旧 Muse/Llama Server Manager 配置迁移时覆盖当前配置或漏掉高级字段差异的问题。
- 修复未知参数因前缀相同而被误删，以及自定义命令切回表单模式后丢失的问题。
- 修复停止/重启竞态、重复关闭、旧会话退出覆盖新会话，以及端口仍占用时配置永久锁定的问题。
- 修复模型长时间加载被误判为超时停止；`/health` 返回 503 时保持加载状态，3 分钟后仅提醒而不自动终止。
- 修复仪表卡片圆角外露直角底板、服务按钮换行错位和部分高 DPI 布局问题。
- 修复 Ollama 等 llama-server 构建无法读取中文目录下 `--api-key-file`，并修正该错误被误诊为模型损坏的问题。

### Tests

- 90 项离线回归覆盖迁移、TurboQuant 能力矩阵、未知参数保留、并发生命周期、端口释放、API Key 中文路径兼容和运行时定位。
- 22 个 UI 场景覆盖七页顶部/底部、940×600、1320×840、浅色/深色和 125%/150%/175%/200% DPI。

## v0.3.0-dev - 2026-08-11

私有开发版本，尚未发布。

### Added

- 产品品牌升级为 LlamaLift，并使用“本地模型，一键起飞。”作为统一副标题。
- 系统性能实时监测：CPU、内存、GPU/显存、服务进程、磁盘、网络与 90 秒交互趋势图。
- llama-server 性能实时监测：吞吐、累计 tokens、处理/排队请求、并发槽位、上下文、高水位和运行时长。
- `/metrics` 启动开关、命令双向解析与旧配置迁移；`/slots` 不依赖额外开关即可提供降级监测。
- 可编辑的完整启动命令工作台与命令到简易表单的反向同步。
- 长短参数、等号语法、带空格路径、布尔值和跨行命令解析。
- 保存前启动参数静态预检，覆盖语法、路径、端口、参数组合、内存风险与局域网鉴权，并提供可执行的修改建议。
- 风险提示采用非阻断式确认；用户仍可保存原始自定义命令，实际启动时不会被静默改写。
- API Key 管理器，支持创建、随机生成、导入、脱敏预览、显隐切换、选择和删除托管密钥文件。
- 未识别参数与完整自定义命令的无损保留。
- 三个默认参数预设槽，支持应用、覆盖保存和自定义名称。
- 参数预设与模型路径、程序路径、网络端点隔离。
- 基于 UI/UX Pro Max 规则的 Apple 式极简桌面 UI 重构。

### Changed

- 默认新安装使用浅色主题；深色与跟随系统模式仍可切换。
- 使用 Apple 中性色板、低饱和胶囊导航、16px 圆角分组卡片、单页面单主操作和明确的输入焦点状态。
- 运行日志和命令编辑器统一使用高对比度深色代码区；日志增加 `[INFO]`、`[WARN]`、`[ERROR]` 文本级别并取消横向滚动。
- 字体全部采用离线系统字体；代码区优先 Cascadia Mono，并提供 Cascadia Code/Consolas 回退。
- 启用 Per-Monitor V2 DPI 感知；侧栏品牌、页面标题和模型配置卡改为可缩放布局，避免高 DPI 下文字与输入框被遮挡。

### Tests

- 新增命令别名、解析失败、未知参数保留、往返一致性和参数预设隔离测试。
- UI 审查覆盖七页顶部/底部状态、实时监测图表、API Key 管理器、940×600、1320×840、浅色/深色和 125%/150%/175%/200% DPI。

## v0.2.0-dev - 2026-08-10

私有开发版本，尚未发布。

### Added

- llama.cpp 官方 Release 发现、Windows x64 构建筛选和硬件后端推荐。
- CPU、CUDA 12/13、Vulkan、SYCL、HIP 运行时下载、校验、安全解压、登记与切换。
- CPU、内存、GPU、显存和 NVIDIA 驱动检测。
- GGUF v2/v3 关键元数据解析与模型量化识别。
- 快速、均衡、极限三档参数自适应。
- 线程、batch、ubatch 命令生成与手工覆盖控件。
- 官方 API 联网集成测试和合成 GGUF 元数据测试。

### Security

- 下载来源限制为 GitHub HTTPS 域名。
- 支持 GitHub Release `sha256:` 摘要验证。
- 采用临时目录安装，并阻止 ZIP 路径穿越；验证 `llama-server.exe` 后才登记运行时。
- GitHub API 和资产下载提供三次有限重试，失败后不破坏现有配置。

## v0.1.0-internal - 2026-08-10

首个私有内测基线。

### Added

- 多套 llama.cpp 模型配置管理。
- llama-server 进程启动、停止、重启与托盘驻留。
- 兼容 API 健康检查和双接口测试。
- 实时日志、运行状态与速度指标。
- 浅色、深色、系统主题和多种强调色。
- 便携版与 Inno Setup 安装版构建流程。
- 离线逻辑测试和多 DPI UI 冒烟测试。

### Security

- 默认仅监听 `127.0.0.1`。
- 仓库不包含模型、密钥、用户配置、运行日志或真实服务器地址。
- 移除早期专用 RTX 3090/Qwen 配置和机器相关路径。
